# Board game graphing.
`data/pyvis`

Stores here plots made by `pyvis`.

**Essential**:

A `./lib` directory, that allow displaying of the `pyvis` web pages.
It's generated by running a Python script that itself runs:
```python
pyvis.network.Network().show()
```
